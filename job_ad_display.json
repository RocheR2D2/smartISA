{
    "$schema": "https://github.com/jobteaser/data-tracking-plan/blob/master/job_ads/job_ad_display.json",
    "title": "Jsonschema for topic job_ads event display",
    "description": "Job Similar Only, see frontend.jobOffers.display otherwise.This topic is intended to centralise all display of job ads, along with the origin of this display.Currently only applies to job similar.",
    "type": "object",
    "anyOf": [
        {"required": ["event_name", "job_ad_id", "initial_job_ad_id"]}
    ],
    "properties": {
        "event_name": {"type": "string", "enum": ["job_ad_display"], "description": "Name of the event"},
        "job_ad_id": {"type": "number", "description": "ID of the displayed ad"},
        "initial_job_ad_id": {"type": "number", "description": "Job Similar - which job ID we were on when this was displayed."}
    }
}
